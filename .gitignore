# ============================================================================
# OpenRM 忽略规则
#
# 原则: 手工资产与生成器必须入库, 生成产物与第三方二进制数据不入库。
#       (生成产物刻意入库 —— 保证新克隆无需 ROM 即可构建, 见文末说明。)
# ============================================================================

# ---- macOS 系统文件 ----
.DS_Store

# ---- 本地工具 / 编辑器数据 ----
.workbuddy/
.zcode

# ---- 构建产物 ----
build/
Testing/

# ---- Python 缓存 ----
__pycache__/
*.pyc

# ---- 第三方 ROM / 参考数据 (体积与授权原因不入库) ----
# 这些是 tools/gen_*.py 的输入, 只在"重新生成头文件"时才需要, 构建不需要。
# 需重新生成时请自备 MAME 驱动已核对 CRC 的 ROM 放入下列目录,
# 再按各脚本头部注释的用法运行 (例: assets/s14001rom/berzerk/*.1c)。
assets/s14001rom/
assets/snsrom/
assets/DECtalk501.pdf

# ----------------------------------------------------------------------------
# 以下三类是"生成产物", 均已入库, 新克隆可直接构建:
#
#   plugins/*/resources/fonts/Mixed-*.ttf
#       <- plugins/*/scripts/subset_font.py   (由 assets/fonts/ 裁剪合成)
#   plugins/*/resources/*.icns | *.ico
#       <- scripts/make_icon_*.py             (由 assets/fonts/Outfit-Bold.ttf 生成)
#   plugins/Narrator/src/dsp/{sp0256,s14001,cts256a,tms}/*.h
#       <- tools/gen_*.py                     (由 assets/ 下第三方 ROM 生成)
#
# 注意: 改动字体或界面文案后, 必须重跑对应生成器并提交产物,
#       否则会出现缺字或图标不一致。
# ----------------------------------------------------------------------------
